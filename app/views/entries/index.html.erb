<div class="row">
  <div class="col-md-offset-2 col-md-8">
    <%  @entries.each do |e| %>
    <h2><%= link_to e.created_at, entry_path(e)%></h2>
    <p>
      <%= e.content %>
    </p>
    <% end %>
  </div>
</div>
